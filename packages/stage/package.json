{
  "name": "@bhoos/stage",
  "version": "0.0.3",
  "description": "An animation sequencing library",
  "main": "__dist__/index.js",
  "author": "Ranjan Shrestha",
  "license": "MIT",
  "files": [
    "__dist__"
  ],
  "scripts": {
    "clean": "../../node_modules/.bin/rimraf __dist__",
    "flow": "../../node_modules/.bin/flow-copy-source src __dist__",
    "build": "../../node_modules/.bin/babel src -d __dist__",
    "prepack": "npm run clean && npm run build && npm run flow",
    "prepublishOnly": "npm run prepack",
    "watch": "../../node_modules/.bin/babel src -d __dist__ -w"
  },
  "peerDependencies": {
    "react": "^16.3.0",
    "react-native": "^0.54.4"
  }
}
